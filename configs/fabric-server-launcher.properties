# ============================================================================
# 🕸️ Configuración del Launcher de Fabric Server
# Versión: 1.0
# Propósito: Configuraciones específicas del launcher de Fabric
# ============================================================================

# === CONFIGURACIÓN DEL LAUNCHER ===
# Archivo JAR del servidor de Minecraft
serverJar=server.jar

# Argumentos JVM personalizados (se concatenan con los del script)
# Deja vacío si usas el iniciador_optimizado_de_minecraft.sh
jvmArgs=

# Argumentos del programa (argumentos pasados al servidor)
programArgs=nogui

# === CONFIGURACIÓN DE FABRIC ===
# Versión del loader de Fabric
fabricVersion=0.15.6

# Versión de Minecraft
minecraftVersion=1.20.4

# === CONFIGURACIÓN DE LOGGING ===
# Nivel de logging (ALL, TRACE, DEBUG, INFO, WARN, ERROR, OFF)
loggingLevel=INFO

# Configurar logging específico
log4j.configurationFile=log4j2_server.xml

# === CONFIGURACIÓN DE DESARROLLO ===
# Habilitar modo desarrollo (solo para desarrolladores de mods)
fabric.development=false

# Habilitar remapping en tiempo real
fabric.remapClasspathFile=false

# === CONFIGURACIÓN DE RENDIMIENTO ===
# Skip mod provider cuando sea posible
fabric.skipMcProvider=true

# Deshabilitar verificaciones innecesarias en producción
fabric.gameJarProcessing.publishDependencies=false

# === CONFIGURACIÓN DE MODS ===
# Directorio de mods personalizado (relativo al directorio del servidor)
fabric.modsDir=mods

# Cargar mods desde subdirectorios
fabric.loadNestedJars=true

# === CONFIGURACIÓN DE COMPATIBILIDAD ===
# Modo de compatibilidad con Forge (experimental)
fabric.forgeCompatibility=false

# Deshabilitar verificación de firma de mods
fabric.skipSignatureCheck=false

# === CONFIGURACIÓN DE NETWORK ===
# Configuraciones de red específicas de Fabric
fabric.network.threads=4

# === CONFIGURACIÓN DE CACHE ===
# Directorio de cache
fabric.cacheDir=.fabric/cache

# Limpiar cache en inicio
fabric.clearCache=false

# === CONFIGURACIÓN AVANZADA ===
# Configuraciones experimentales (usar con precaución)
fabric.experimental.enableExperimentalFeatures=false

# === VARIABLES DE ENTORNO RECOMENDADAS ===
# Estas se pueden configurar como variables de entorno del sistema:
# FABRIC_DEVELOPMENT=false
# FABRIC_SKIP_MC_PROVIDER=true
# FABRIC_MODS_DIR=mods